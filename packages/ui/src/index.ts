/**
 * @panchangpal/ui — CMP_* component library (PDD Part 3 §5). Components compose
 * @panchangpal/design-tokens and carry accessibility, loading, disabled, and error
 * states per PDD §10 (accessibility is release-blocking, ADR-029). Components are
 * added during the design-system + component-build tasks; this is the package seam.
 */
export {};
