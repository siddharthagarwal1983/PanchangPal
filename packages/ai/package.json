{
  "name": "@panchangpal/ai",
  "version": "0.1.0",
  "private": true,
  "description": "Server-shared RAG helpers: prompt builder, scope guardrails, retrieval. Used by SVC_ask_guru + SVC_content_ingest. Server-only — never imported by the mobile app (ADR-006/011).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "build": "tsc"
  },
  "dependencies": {
    "@panchangpal/shared": "workspace:*"
  }
}
