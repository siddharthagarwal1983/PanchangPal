# System Files
.DS_Store
Thumbs.db

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node / Dependencies
node_modules/
/dist
# Built package output — regenerated by `turbo run build` (^build); never commit.
dist/
packages/*/dist/
/build
.next/
out/
.docusaurus/
.turbo/
.pnpm-store/

# Environment / Secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.env
.env.gpg
.env.*
*.env

# Keep committed env TEMPLATES (placeholders only — never real secrets).
# These negations must stay AFTER the ignore rules above so the examples are tracked.
!.env.example
!.env.local.example
!.env.development.example
!.env.staging.example
!.env.production.example
!.env.ci.example

# Supabase
supabase/.temp/
supabase/.branches/
supabase/functions/.env

# Expo local state (generated by `expo start`; per-machine, never shared)
apps/mobile/.expo/

# Native projects. This is a managed Expo app: android/ and ios/ are GENERATED by
# `expo prebuild` (in e2e.yml per run, and locally when reproducing it) and are not the
# source of truth — app.config.ts is. Committing them would let the two drift.
apps/mobile/android/
apps/mobile/ios/

# Stray root Expo config. The app config lives at apps/mobile/app.config.ts; eas-cli
# regenerates an empty app.json here whenever it is invoked from the repository root,
# where it would shadow the real config.
/app.json

# Claude Code per-developer settings (shared config stays in .claude/settings.json)
.claude/settings.local.json

# JetBrains IDE
.idea/

# SIGNING MATERIAL — never commit. A keystore is the app's signing identity: anyone holding
# it can produce a build the OS accepts as a genuine update. EAS stores these; a local copy
# belongs in a password manager or encrypted backup, never in version control.
*.jks
*.keystore
*.p12
*.p8
*.mobileprovision
*.cer

# `eas credentials` writes credentials.json next to the keystore it downloads, holding the
# store and key passwords in PLAINTEXT. The keystore itself is covered above; this file was
# not, and a password file is worth as much as the key once both are in the same history.
credentials.json
credentials/

# Temporary / OS
.tmp/
temp/
.env
.mcp.json
