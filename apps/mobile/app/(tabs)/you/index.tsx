/**
 * SCR_PROFILE_001 — You (MOD_you). Empty shell; profile, household, achievements,
 * subscription, settings, and account deletion are built in the You feature task.
 */
import { View, Text } from 'react-native';

export default function YouScreen() {
  return (
    <View>
      <Text accessibilityRole="header">You</Text>
    </View>
  );
}
