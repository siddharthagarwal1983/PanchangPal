/**
 * SCR_GURU_HOME_001 — Ask Guru (MOD_guru). Empty shell; the grounded-or-silent
 * streaming chat client (SSE, TDD Part 4 §7.1) is built in the Ask Guru feature task.
 */
import { View, Text } from 'react-native';

export default function GuruScreen() {
  return (
    <View>
      <Text accessibilityRole="header">Ask Guru</Text>
    </View>
  );
}
