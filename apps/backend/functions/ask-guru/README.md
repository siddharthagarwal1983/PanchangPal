# ask-guru

**Placeholder — no business logic implemented (Repository Scaffolding task).**

SVC_ask_guru — API_POST_ASK_GURU. Grounded-or-silent RAG over pgvector, streamed SSE (ADR-004/019). Server-only OpenAI access (ADR-006/011).

Implemented in the Backend Foundation milestone. Stateless + idempotent; secrets are
server-only (ADR-006/030).
